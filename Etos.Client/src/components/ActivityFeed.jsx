import { Stack, Typography } from "@mui/material";

export default function ActivityFeed() {
    return (
        <Stack>
            <Typography variant="h3">
                Training
            </Typography>
            <Typography variant="h3">
                Orientation
            </Typography>
            <Typography variant="h3">
                Feedback
            </Typography>
        </Stack>
    );
}
